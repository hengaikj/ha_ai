# 后端开发启动说明

后端执行已批准 Engineering Handoff，不重新做业务发现。

技术基线：Java、Spring Boot、MyBatis-Plus、MySQL 8、Redis；Java根包 `com.hengaikj.ai`；模块前缀 `ha-`；表前缀 `ha_`；Redis `ha:{env}:`；金额/价格/费率 `DECIMAL(24,12)` + BigDecimal；HALF_UP；数据库表和字段必须中文 COMMENT。

开发顺序按 Vertical Slice，不按 Controller/Mapper 横向铺满。M01 优先完成真实 Gateway 链路，Billing/Fallback/Streaming/Payment 后置。
