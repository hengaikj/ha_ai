# M01 后端输入阻塞证据

## 任务

- 任务编号：BE-M01-001
- 分支：`feature/m01-slice01`
- 工作目录：`backend-worktree`
- 记录日期：2026-09-22

## 已读取输入

已按开工指令顺序读取项目公共规范、Baseline 与变更管理规范、Git/PR 规范、安全与 Secret 规范、后端启动说明、后端执行 Skill、M01 后端任务包，以及当前 `.project/*.yaml` 任务板。

## 阻塞事实

`contracts/` 目前只有 `README.md`，缺少 M01 所需的机器可读 OpenAPI、Database、Redis、Test Contract。README 仅列出计划目录，没有定义接口字段、鉴权语义、数据库表结构、Redis Key 或测试断言。

根据开工指令和 Baseline 规范，正式 Contract 缺失时不得自行编造，必须标记 UNKNOWN 并停止将猜测实现为正式规则。因此 BE-M01-001 暂停在输入核验阶段，未编写后端业务代码。

## 验证证据

| 项目 | 结果 |
|---|---|
| Maven Build | NOT_RUN（当前 `backend/` 仅有 README，无 Maven 工程） |
| Unit Test | NOT_RUN |
| API Test | NOT_RUN |
| API Key 禁用/撤销/过期 | NOT_RUN |
| Redis miss → MySQL fallback | NOT_RUN |
| Model Policy 授权/拒绝 | NOT_RUN |
| FakeProvider 成功/失败 | NOT_RUN |
| `GET /v1/models` | NOT_RUN |
| `POST /v1/chat/completions` | NOT_RUN |
| DEV 验证 | NOT_RUN |
| 真实 HengAi Provider 验证 | NOT_RUN（无已批准 DEV 凭证） |

## 范围与偏离

- Billing、Ledger、Fallback、Streaming、支付宝、微信支付：未实现。
- 未修改 `.project` 状态，未自行标记 VERIFIED。
- Baseline 偏离：NONE；本记录仅报告输入缺口。
- Secret/Provider Credential：未读取、未生成、未写入。

## 待解锁条件

请项目负责人导入并批准 M01 对应的 OpenAPI、Database、Redis、Test Contract（以及必要的 Engineering Handoff Requirement/AC/Technical Baseline）。输入齐备后再继续 BE-M01-001，并由 Reviewer 根据 Commit、PR、CI/Test Evidence 审查。
