# 前端开发启动说明

前端以 Design Baseline + OpenAPI 为权威，不根据页面方便程度自行发明业务规则。

建议当前技术实现围绕企业端与平台运营端展开；接口未就绪时使用与OpenAPI一致的Mock，真实API可用后切换，不维护两套字段语义。

所有页面必须处理 Loading、Empty、Error、Forbidden、Disabled 等真实状态。API Key完整Secret只允许创建成功时一次性展示，REVOKED不可提供重新启用入口。
