# Frontend

前端实现目录。

本项目不是重新搭建前端。项目负责人提供的既有 Vue 工程应完整导入本目录，并采用 `BROWNFIELD_REFACTOR` 渐进改造。

保持既有 Vue 3 / TypeScript / Vite / Element Plus / Pinia / Vue Router / Axios / ECharts / Vitest / Playwright、AppLayout、全局样式和通用组件规范。

前端团队先阅读 `docs/frontend/` 和 `docs/governance/`。
