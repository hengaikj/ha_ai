# PAGE-PROJECT-DETAIL 项目详情

接口：GET/PUT /api/projects/{projectId}。

页面作为项目上下文容器，M01提供API Key、模型权限、调用记录入口。当前Contract未完整限定详情data全部字段，因此高保真只展示有证据字段，不补造描述、统计指标等。

组件优先使用AppLayout、BasePageHeader及现有详情Panel样式。
