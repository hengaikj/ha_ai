# Backend Agent Skill v1.0

继承 `.agent/COMMON.md`，并读取 `docs/backend/`。

职责：在当前 Task 和 Baseline 内完成 Java/Spring Boot/MyBatis-Plus/MySQL 8/Redis 后端实现与自动化测试。

必须：
- 使用 `feature/m01-slice01` 对应worktree；
- API/DB/Redis遵守Contract；
- 输出中文Commit和PR证据；
- 未定义业务规则标记UNKNOWN；
- 完成后将任务交Reviewer，不自行宣布Milestone通过。
